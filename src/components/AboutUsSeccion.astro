---
import StatsSeccion from "./StatsSeccion.astro";
---

<section id="about-us" class="bg-white dark:bg-dark-800 w-full text-center relative transition-colors duration-300">
  <!-- Versión Móvil Mejorada -->
  <div class="lg:hidden flex flex-col min-h-screen">
    <div class="relative pt-12 px-6 pb-8">
      <!-- Encabezado con efecto especial -->
      <div class="relative inline-block mb-8">
        <span
          class="absolute -top-4 -left-4 w-12 h-12 rounded-full bg-yellow-400/20 dark:bg-yellow-500/20 animate-pulse"
        ></span>
        <h2 class="text-4xl font-bold text-black dark:text-white relative z-10">
          About <span class="text-yellow-500 dark:text-yellow-400">Us</span>
        </h2>
        <div
          class="absolute bottom-0 left-0 w-full h-1 bg-gradient-to-r from-yellow-400 to-yellow-600 dark:from-yellow-500 dark:to-yellow-400 rounded-full"
        >
        </div>
      </div>

      <!-- Texto con mejor jerarquía -->
      <div class="text-left space-y-4">
        <p class="text-gray-600 dark:text-gray-300 leading-relaxed">
          <span class="block text-lg font-medium text-black dark:text-white mb-2">Family-Owned Business</span>
          ACT Construction & Water Solutions has been serving Hawaii for over 10 years with pride and dedication.
        </p>

        <div class="bg-gray-50 dark:bg-dark-700 p-4 rounded-lg border-l-4 border-yellow-400 dark:border-yellow-500">
          <p class="text-gray-600 dark:text-gray-300 italic">
            "Founded by my father, we continue his legacy of excellence in excavation and water solutions."
          </p>
        </div>

        <ul class="grid grid-cols-2 gap-3 mt-6">
          <li class="flex items-center">
            <svg class="w-5 h-5 text-yellow-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            <span class="text-sm text-gray-600 dark:text-gray-300">Excavation</span>
          </li>
          <li class="flex items-center">
            <svg class="w-5 h-5 text-yellow-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            <span class="text-sm text-gray-600 dark:text-gray-300">Demolition</span>
          </li>
          <li class="flex items-center">
            <svg class="w-5 h-5 text-yellow-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            <span class="text-sm text-gray-600 dark:text-gray-300">Trenching</span>
          </li>
          <li class="flex items-center">
            <svg class="w-5 h-5 text-yellow-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            <span class="text-sm text-gray-600 dark:text-gray-300">Water Delivery</span>
          </li>
        </ul>
      </div>
    </div>

    <!-- Imagen con estadísticas (versión móvil) -->
    <div
      class="relative flex-grow bg-cover bg-center"
      style="background-image: url('images/imageAboutUs.webp'); min-height: 400px;"
    >
      <div class="absolute inset-0 bg-black/40 dark:bg-dark-900/60"></div>
      <div class="relative z-10 h-full flex flex-col justify-end pb-8 px-6">
        <div class="bg-white/90 dark:bg-dark-700/90 backdrop-blur-sm p-6 rounded-xl shadow-lg">
          <StatsSeccion />
        </div>
      </div>
    </div>
  </div>

  <!-- Versión Desktop (se mantiene igual) -->
  <div class="hidden lg:block z-30 relative lg:h-screen lg:min-h-[700px] flex-col">
    <header class="flex flex-col items-center pt-8 lg:pt-[130px] px-6 sm:px-8">
      <div class="relative inline-block pb-2">
        <h2 class="text-3xl lg:text-4xl font-bold text-black dark:text-white transition-colors duration-300">
          About us
        </h2>
        <div
          id="underline"
          class="absolute bottom-0 left-1/2 transform -translate-x-1/2 w-[70%] h-1 bg-yellow-400 dark:bg-yellow-500 origin-center scale-x-0"
        >
        </div>
      </div>

      <p
        class="text-sm lg:text-base text-justify text-gray-600 dark:text-gray-300 pt-6 lg:pt-[50px] px-0 sm:px-4 lg:px-[500px] font-sans transition-colors duration-300"
      >
        ACT Construction & Water Solutions is a family-owned business with over 10 years of experience. Founded b~y the
        father of our current owner, we proudly continue his legacy by providing top-tier excavation, grading,
        demolition, trenching, and land clearing services using high-quality machinery.<br /><br />
        To better serve our local community, we now offer non-potable water delivery for irrigation, cleaning, construction,
        and more. With a strong commitment to excellence, we take pride in serving the Hawaiian community with reliability
        and dedication.
      </p>
    </header>

    <footer
      class="flex-grow flex justify-center items-end pb-8 lg:pb-10 w-full lg:h-1/3 mt-8 lg:mt-0 bg-cover bg-center lg:absolute lg:bottom-0 overflow-hidden relative"
      style="background-image: url('images/imageAboutUs.webp'); min-height: 300px;"
    >
      <div class="absolute inset-0 bg-black/50 dark:bg-dark-900/70 transition-colors duration-300"></div>
      <div class="relative z-20 w-full py-6 lg:py-0">
        <StatsSeccion />
      </div>
    </footer>
  </div>

  <style>
    /* Animaciones mejoradas */
    @keyframes lineBounce {
      0% {
        transform: translateX(-50%) scaleX(0);
        height: 2px;
        bottom: -5px;
      }
      60% {
        transform: translateX(-50%) scaleX(1.2);
        height: 5px;
        bottom: -8px;
      }
      100% {
        transform: translateX(-50%) scaleX(1);
        height: 3px;
        bottom: -6px;
      }
    }
    @keyframes pulse {
      0%,
      100% {
        opacity: 0.3;
      }
      50% {
        opacity: 0.6;
      }
    }
    .animate-underline {
      animation: lineBounce 0.8s cubic-bezier(0.68, -0.55, 0.27, 1.55) forwards;
    }
    .animate-pulse {
      animation: pulse 2s infinite;
    }

    /* Transiciones suaves */
    .transition-all {
      transition-property: all;
      transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
      transition-duration: 300ms;
    }
  </style>

  <script>
    document.addEventListener("DOMContentLoaded", function () {
      const aboutUsSection = document.getElementById("about-us");
      const underline = document.getElementById("underline");

      if (aboutUsSection && underline) {
        const observer = new IntersectionObserver(
          function (entries) {
            entries.forEach(function (entry) {
              if (entry.isIntersecting) {
                underline.classList.add("animate-underline");
                observer.unobserve(entry.target);
              }
            });
          },
          { threshold: 0.2 }
        );
        observer.observe(aboutUsSection);
      }
    });
  </script>
</section>
