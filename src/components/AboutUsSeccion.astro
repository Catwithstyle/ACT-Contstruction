---
import StatsSeccion from './StatsSeccion.astro';
---
<section id="about-us" class="bg-white dark:bg-gray-900 w-full overflow-hidden transition-colors duration-300">
  <!-- ===== VERSIÓN MÓVIL ===== -->
  <div class="lg:hidden py-12 px-6">
    <!-- Sección de contenido -->
    <div class="mb-10">
      <!-- Título con decoración -->
      <div class="mb-8 text-center">
        <span class="inline-block mb-3 text-sm font-semibold text-yellow-600 dark:text-yellow-400">OUR STORY</span>
        <h2 class="text-3xl font-bold text-gray-900 dark:text-white">Building Hawaii's Future</h2>
        <div class="mx-auto mt-3 w-16 h-1 bg-gradient-to-r from-yellow-400 to-yellow-500 rounded-full"></div>
      </div>

      <!-- Contenido principal -->
      <div class="space-y-6">
        <!-- Tarjeta 1 -->
        <div class="bg-gray-50 dark:bg-gray-800 p-6 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700">
          <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3">Family Legacy</h3>
          <p class="text-gray-700 dark:text-gray-300 leading-relaxed">
            Founded over 10 years ago by the Rodriguez family, ACT Construction has been serving Hawaii with pride and
            dedication.
          </p>
        </div>

        <!-- Cita destacada -->
        <div class="border-l-4 border-yellow-400 pl-4 py-3 my-6 bg-gray-50 dark:bg-gray-800 rounded-r-lg">
          <p class="text-gray-600 dark:text-gray-300 italic">
            "My father taught us that quality work builds lasting relationships. That's the foundation of our business."
          </p>
        </div>

        <!-- Descripción de la empresa -->
        <div class="bg-gray-50 dark:bg-gray-800 p-6 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700">
          <p class="text-gray-700 dark:text-gray-300 leading-relaxed">
            ACT Construction & Water Solutions is a family-owned business with deep roots in Hawaii's construction
            industry. Founded over a decade ago, we've grown from a small local operation to a trusted name in
            excavation and water services.
          </p>
        </div>

        <!-- Servicios en grid -->
        <div class="grid grid-cols-2 gap-4 mt-6">
          <div
            class="bg-gray-50 dark:bg-gray-800 p-4 rounded-lg flex items-center border border-gray-200 dark:border-gray-700"
          >
            <div class="w-8 h-8 bg-yellow-100 dark:bg-yellow-900/30 rounded-full flex items-center justify-center mr-3">
              <svg class="w-4 h-4 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <span class="text-sm font-medium text-gray-700 dark:text-gray-300">Excavation</span>
          </div>

          <div
            class="bg-gray-50 dark:bg-gray-800 p-4 rounded-lg flex items-center border border-gray-200 dark:border-gray-700"
          >
            <div class="w-8 h-8 bg-yellow-100 dark:bg-yellow-900/30 rounded-full flex items-center justify-center mr-3">
              <svg class="w-4 h-4 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <span class="text-sm font-medium text-gray-700 dark:text-gray-300">Demolition</span>
          </div>

          <div
            class="bg-gray-50 dark:bg-gray-800 p-4 rounded-lg flex items-center border border-gray-200 dark:border-gray-700"
          >
            <div class="w-8 h-8 bg-yellow-100 dark:bg-yellow-900/30 rounded-full flex items-center justify-center mr-3">
              <svg class="w-4 h-4 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <span class="text-sm font-medium text-gray-700 dark:text-gray-300">Trenching</span>
          </div>

          <div
            class="bg-gray-50 dark:bg-gray-800 p-4 rounded-lg flex items-center border border-gray-200 dark:border-gray-700"
          >
            <div class="w-8 h-8 bg-yellow-100 dark:bg-yellow-900/30 rounded-full flex items-center justify-center mr-3">
              <svg class="w-4 h-4 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <span class="text-sm font-medium text-gray-700 dark:text-gray-300">Water Delivery</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Sección de stats -->
    <div class="bg-gray-900 p-6 rounded-xl">
      <StatsSeccion />
    </div>
  </div>

  <!-- ===== VERSIÓN DESKTOP ===== -->
  <div class="hidden lg:block py-20 px-8">
    <div class="max-w-6xl mx-auto">
      <!-- Título -->
      <div class="text-center mb-16">
        <span class="text-sm font-semibold text-yellow-600 dark:text-yellow-400">OUR STORY</span>
        <h2 class="text-4xl font-bold text-gray-900 dark:text-white mt-3">
          About <span class="text-yellow-500">Our Company</span>
        </h2>
        <div class="mx-auto mt-4 w-24 h-1.5 bg-gradient-to-r from-yellow-400 to-yellow-500 rounded-full"></div>
      </div>

      <!-- Contenido principal -->
      <div class="grid grid-cols-2 gap-12 items-center">
        <!-- Texto -->
        <div class="space-y-8">
          <div class="space-y-6">
            <h3 class="text-2xl font-bold text-gray-900 dark:text-white">Family Legacy</h3>
            <p class="text-lg text-gray-700 dark:text-gray-300 leading-relaxed">
              ACT Construction & Water Solutions is a family-owned business with deep roots in Hawaii's construction
              industry. Founded over a decade ago, we've grown from a small local operation to a trusted name in
              excavation and water services.
            </p>
          </div>

          <!-- Cita -->
          <div class="border-l-4 border-yellow-400 pl-6 py-4 bg-gray-50 dark:bg-gray-800 rounded-r-lg">
            <p class="text-gray-600 dark:text-gray-300 italic text-lg">
              "What started as my father's vision has become our family's mission - to deliver exceptional service
              with aloha spirit."
            </p>
          </div>

          <!-- Servicios -->
          <div class="grid grid-cols-2 gap-4 mt-6">
            <div class="flex items-center">
              <div class="w-10 h-10 bg-yellow-100 dark:bg-yellow-900/30 rounded-full flex items-center justify-center mr-3">
                <svg class="w-5 h-5 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
              </div>
              <span class="font-medium text-gray-700 dark:text-gray-300">10+ Years Experience</span>
            </div>
            <div class="flex items-center">
              <div class="w-10 h-10 bg-yellow-100 dark:bg-yellow-900/30 rounded-full flex items-center justify-center mr-3">
                <svg class="w-5 h-5 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
              </div>
              <span class="font-medium text-gray-700 dark:text-gray-300">200+ Projects</span>
            </div>
            <div class="flex items-center">
              <div class="w-10 h-10 bg-yellow-100 dark:bg-yellow-900/30 rounded-full flex items-center justify-center mr-3">
                <svg class="w-5 h-5 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
              </div>
              <span class="font-medium text-gray-700 dark:text-gray-300">6+ Services</span>
            </div>
            <div class="flex items-center">
              <div class="w-10 h-10 bg-yellow-100 dark:bg-yellow-900/30 rounded-full flex items-center justify-center mr-3">
                <svg class="w-5 h-5 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
              </div>
              <span class="font-medium text-gray-700 dark:text-gray-300">100% Satisfaction</span>
            </div>
          </div>
        </div>

        <!-- Imagen -->
        <div class="relative h-full min-h-[400px] rounded-xl overflow-hidden shadow-2xl">
          <img 
            src="/images/imageAboutUs.webp" 
            alt="Our Team" 
            class="w-full h-full object-cover object-center"
          />
          <div class="absolute inset-0 bg-gradient-to-t from-black/40 via-black/10 to-transparent"></div>
        </div>
      </div>

      <!-- Stats Section -->
      <div class="mt-20 bg-gray-900 rounded-xl p-10">
        <StatsSeccion />
      </div>
    </div>
  </div>

  <style>
    /* Animaciones */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Efecto de aparición */
    .animate-fade-in {
      animation: fadeIn 0.8s ease-out forwards;
    }
  </style>
</section>